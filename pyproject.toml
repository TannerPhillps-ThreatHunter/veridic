[project]
name = "veridic"
version = "0.2.0"
description = "A semantic computation system for meaning-preserving computation"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

dependencies = []

[tool.veridic]
runtime = "stdlib-only"
dependency_policy = "zero-third-party"
